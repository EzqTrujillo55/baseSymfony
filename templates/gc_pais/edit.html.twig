{% extends 'base.html.twig' %}

{% block title %}Edit GcPais{% endblock %}

{% block body %}
    <h1>Edit GcPais</h1>

    {{ include('gc_pais/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('gc_pais_index') }}">back to list</a>

    {{ include('gc_pais/_delete_form.html.twig') }}
{% endblock %}
