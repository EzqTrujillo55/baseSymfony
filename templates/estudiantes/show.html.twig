{% extends 'base.html.twig' %}

{% block title %}Estudiantes{% endblock %}

{% block body %}
    <h1>Estudiantes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estudiante.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ estudiante.name }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ estudiante.apellido }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ estudiante.email }}</td>
            </tr>
            <tr>
                <th>Nacimiento</th>
                <td>{{ estudiante.nacimiento ? estudiante.nacimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cedula</th>
                <td>{{ estudiante.cedula }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estudiantes_index') }}">back to list</a>

    <a href="{{ path('estudiantes_edit', {'id': estudiante.id}) }}">edit</a>

    {{ include('estudiantes/_delete_form.html.twig') }}
{% endblock %}
