{% extends 'base.html.twig' %}

{% block title %}New GcPerfil{% endblock %}

{% block body %}
    <div class="jumbotron mt-2 container">
    <h1>Crear Perfil</h1>

    {{ include('gc_perfil/_form.html.twig') }}
    </div>
  <!--  <a href="{{ path('gc_perfil_index') }}">back to list</a> -->
{% endblock %}
