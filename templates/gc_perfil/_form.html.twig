{{ form_start(form) }}
    {{ form_errors(form) }}
<div class="form-group row">
    <div class="col-sm-2">
        {{ form_label(form.empresa, 'Empresa:') }}
    </div>
    <div class="col-sm-10">
        {{ form_errors(form.empresa) }}
        {{ form_widget(form.empresa) }}
    </div>
</div>

<div class="form-group row">
    <div class="col-sm-2">
        {{ form_label(form.prNombre, 'Perfil:') }}
    </div>
    <div class="col-sm-10">
        {{ form_errors(form.prNombre) }}
        {{ form_widget(form.prNombre) }}
    </div>
</div>



    {{ form_row(form._token) }}
    <button class="btn btn-primary">{{ button_label|default('Registrar') }}</button>

